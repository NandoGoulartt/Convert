import React from "react";
import { Box } from "@chakra-ui/react";
import LayoutContainer from "../../Componentes/Layout";

const Area = () => {
  return (
    <LayoutContainer>
      <Box p={8}>
        <h1>Conversão de area</h1>
        <p>Isso é um exemplo de como usar Conversão de area.</p>
      </Box>
    </LayoutContainer>
  );
};

export default Area;
